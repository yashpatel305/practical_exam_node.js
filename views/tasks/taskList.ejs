<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= title %></title>
  <link rel="stylesheet" href="/css/style.css" />
</head>
<body>
  <%- include('../partials/navbar') %>

  <main class="container">
    <h1><%= title %></h1>
    <% if (!tasks.length) { %>
      <p>No tasks available.</p>
    <% } else { %>
      <ul class="task-list">
        <% tasks.forEach((task) => { %>
          <%- include('./taskItem', { task }) %>
        <% }) %>
      </ul>
    <% } %>
  </main>
</body>
</html>
